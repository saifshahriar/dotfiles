- Download Neovim

- Setup `NvChad nvim` config: https://nvchad.github.io/getting-started/setup

- Now add the following lines from the `init.lua` (dotfiles') to the `init.lua` file.

     ⚠ Do not overwrite the `init.lua` file
         
- Copy `.vimrc.vim` to the `nvim` folder.
